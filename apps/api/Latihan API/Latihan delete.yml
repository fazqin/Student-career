info:
  name: Latihan delete
  type: http

http:
  method: DELETE
  url: http://localhost:3000/api/jobs/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
